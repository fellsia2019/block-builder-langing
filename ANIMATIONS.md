# 🎨 Система анимаций

Лендинг использует собственную систему анимаций, основанную на React hooks и IntersectionObserver API.

## 📦 Компоненты

### `useScrollAnimation` Hook

Кастомный React hook для создания анимаций при появлении элементов во viewport.

**Возможности:**
- ✅ IntersectionObserver API для оптимизации
- ✅ 6 типов анимаций (Fade in Up/Down/Left/Right, Zoom in, Scale up)
- ✅ Параллакс эффекты при скролле
- ✅ Настраиваемые задержки и длительность
- ✅ Опция triggerOnce для однократного срабатывания

**Параметры:**

```typescript
interface UseScrollAnimationOptions {
  animationName?: AnimationType;          // Тип анимации
  threshold?: number | number[];          // Порог видимости
  rootMargin?: string;                    // Отступ для observer
  animationTime?: number;                 // Длительность анимации (ms)
  animationDelay?: number;                // Задержка перед анимацией (ms)
  parallaxEnabled?: boolean;              // Включить параллакс
  deltaPercent?: number;                  // Интенсивность параллакса
  animationTimeParallax?: number;         // Длительность параллакса (ms)
  triggerOnce?: boolean;                  // Срабатывать только раз
}
```

### `AnimateOnScroll` Component

Обертка для элементов, которые должны анимироваться при скролле.

**Пример использования:**

```tsx
<AnimateOnScroll 
  animationName="FADE_IN_UP"
  animationDelay={200}
  animationTime={600}
>
  <div>Your content here</div>
</AnimateOnScroll>
```

## 🎭 Типы анимаций

### FADE_IN_UP
Появление снизу вверх с fade эффектом
- Transform: `translateY(50px) → translateY(0)`
- Opacity: `0 → 1`

### FADE_IN_DOWN
Появление сверху вниз с fade эффектом
- Transform: `translateY(-50px) → translateY(0)`
- Opacity: `0 → 1`

### FADE_IN_LEFT
Появление слева направо с fade эффектом
- Transform: `translateX(-50px) → translateX(0)`
- Opacity: `0 → 1`

### FADE_IN_RIGHT
Появление справа налево с fade эффектом
- Transform: `translateX(50px) → translateX(0)`
- Opacity: `0 → 1`

### ZOOM_IN
Увеличение с центра
- Transform: `scale(0.5) → scale(1)`
- Opacity: `0 → 1`

### SCALE_UP
Плавное увеличение
- Transform: `scale(0.8) → scale(1)`
- Opacity: `0 → 1`

## 🌊 Параллакс эффекты

Включите параллакс для создания эффекта глубины:

```tsx
<AnimateOnScroll 
  animationName="FADE_IN_RIGHT"
  parallaxEnabled={true}
  deltaPercent={0.05}
>
  <YourComponent />
</AnimateOnScroll>
```

**Параметры параллакса:**
- `parallaxEnabled` - включить/выключить
- `deltaPercent` - множитель скорости (0.01-0.2 оптимально)
- `animationTimeParallax` - плавность перехода

## 🎯 Где используется

### Hero Section
- Постепенное появление заголовка (FADE_IN_DOWN)
- Подзаголовок (FADE_IN_UP)
- Кнопки (SCALE_UP)
- Бейджи (FADE_IN_UP)

### Features Section
- Заголовок (FADE_IN_DOWN)
- Карточки (циклично: FADE_IN_UP, FADE_IN_LEFT, FADE_IN_RIGHT, ZOOM_IN, SCALE_UP)
- Задержка: 100ms между карточками

### Architecture Section
- Слои слева (FADE_IN_LEFT, задержки 200/400/600ms)
- Диаграмма справа (FADE_IN_RIGHT + параллакс)
- Статистика (ZOOM_IN, задержки 200/400/600ms)

### Code Examples Section
- Заголовок (FADE_IN_DOWN)
- Табы (FADE_IN_UP)
- Блок кода (SCALE_UP)
- Карточки внизу (FADE_IN_UP с разными задержками)

### Installation Section
- Заголовок (FADE_IN_DOWN)
- Блок установки (SCALE_UP)
- Шаги (FADE_IN_LEFT, FADE_IN_UP, FADE_IN_RIGHT)
- Ресурсы (FADE_IN_UP)

## ⚡ Оптимизация производительности

1. **IntersectionObserver** - элементы анимируются только когда появляются во viewport
2. **triggerOnce** - по умолчанию true, анимация срабатывает только один раз
3. **Passive scroll listeners** - не блокируют главный поток
4. **CSS transitions** - аппаратное ускорение через GPU
5. **Will-change** - браузер оптимизирует анимируемые свойства

## 🎨 Кастомизация

Для добавления новой анимации:

1. Добавьте тип в `ANIMATIONS`:
```typescript
export const ANIMATIONS = {
  // ... existing
  YOUR_ANIMATION: 'YOUR_ANIMATION',
}
```

2. Добавьте стили в `ANIMATE_STYLES`:
```typescript
export const ANIMATE_STYLES = {
  // ... existing
  YOUR_ANIMATION: {
    hide: {
      opacity: 0,
      transform: 'your transform',
    },
    show: {
      opacity: 1,
      transform: 'scale(1)',
    },
  },
}
```

3. Используйте в компонентах:
```tsx
<AnimateOnScroll animationName="YOUR_ANIMATION">
  <YourContent />
</AnimateOnScroll>
```

## 📱 Адаптивность

Все анимации работают на всех размерах экранов. На мобильных устройствах параллакс может быть отключен для лучшей производительности.

## 🌐 Браузерная поддержка

- ✅ Chrome 51+
- ✅ Firefox 55+
- ✅ Safari 12.1+
- ✅ Edge 15+

IntersectionObserver поддерживается всеми современными браузерами.

